<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="camp.bso.inf.scorringboard">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash_Screen_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PersibActivity" />
        <activity android:name=".PersijaActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".list_pertandingan_Activity" />
        <activity
            android:name=".NavBarPertandingan"
            android:label="@string/title_activity_nav_bar_pertandingan"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Profile2Activity" />
        <activity android:name=".ProfileActivity"></activity>
    </application>

</manifest>